# Start here — first 45 minutes

1. Open `index.html` (double-click or run `./launch.sh`).
2. Setup: name, city, skills, monthly goal, weekly hours.
3. Save & generate plan.
4. Operator tab → copy Master System Prompt.
5. Paste into a dedicated Grok thread or a Gemini Gem named `Income OS Operator`.
6. Daily tab → copy today’s brief into that same thread.
7. Send the drafts the assistant returns. Log every send in Pipeline.

Do not build a website, logo, or LLC today. First dollar first.
